20/10
React js — библиотека с открытым исходным кодом для разработки пользовательских интерфейсов
 Render () - прорисовка на экране сохранённых изменений 
extends  - наследование от класса
component — компонент 
JSX — расширение java script
reactDom? - дерево прорисовки
как открыть локал хост? - cd ~/FIRUZA/crowdfounding && yarn start 


props — это свойства наследуемые 
state — состояние


ооп- группировка функций и данных в единые сущности — объекты

 класс- нужен для создания объектов и методов
методы-свойства:
set — задать значение
get — получить значение
set on ready — результат на выходе
 is running — запущена ли?
Наследование- создание класса на основе существующих
 

Решать задачи на javascript — node- запустить серверный интерпретатор js
node test.js
верстка — в react 


21/10
console.log - вывести в консоль
alert - всплывающее окно
confirm - окошко с да/нет
prompt - окошко, запрашивающее данные


22/10
src - ссылка на скрипт 
 атрибуты: async (до src="...") - Когда скрипт будет загружен – он выполнится
 defer - загружает скрипты по порядку
 Ctrl+/ для однострочных  Ctrl+Shift+/ – для многострочных комментариев 
 'use strict' - новый стандарт ES5
 var - обьявление переменной (для хранения информации, состоит из имени и области выделенной памяти)
переменные с БОЛЬШИМИ_БУКВАМИ - константы

 
23/10
типы данных: примитивные -  1 - число (n = 123)
                            2 - строка (str)
                            3 - булевые - (true/false)
                            4 - пустой (null)
                            5 - значение не присвоено (indefined- var x)
           6 -  обьекты - для коллекций данных и для объявления более сложных сущностей - {}
           7 -  typeof возвращает тип аргумента (typeof x)
           
    = - присваивание
    % - взятие остатка
    ++ - инкремент - увеличение на единицу (есть префиксная и постфиксная форма)
    -- - декремент - уменьшение на единицу (есть префиксная и постфиксная форма)
побитовые операции: AND(и) ( & )
                    OR(или) ( | )
                    XOR(побитовое исключающее или) ( ^ )
                    NOT(не) ( ~ )
                    LEFT SHIFT(левый сдвиг) ( << )
                    RIGHT SHIFT(правый сдвиг) ( >> )
                    ZERO-FILL RIGHT SHIFT(правый сдвиг с заполнением нулями) ( >>> )
                     
24/10
сокращенное присваение - +=, -=, *=, /=, %= и так же <<,>>,>>>,&,|,^.
оператор  - , промежуточное значение либо перечисление
операторы сравнения: a == b - сравнение
=== - строгое равенство
!= - не равно
лексикографическое - сравнение букв
null
underfind 
NaN- not a number
побитовые операции:
parseInt("11000", 2) - переводит строку с двоичной записью числа в число.
n.toString (2) - получает для числа n запись в 2-ной системе в виде строки.

+ примерные соображения по верстке

25/10
alert - модальное окно
prompt("текст", то_что_вводит_пользователь) - модальное окно 
result = confirm(question);
(confirm - выводит окно с вопросом question с двумя кнопками: OK и CANCEL.
Результатом будет true при нажатии OK и false – при CANCEL(Esc))

26/10
условные операторы: if(условие) - если(условие верно), то {}, else - иначе
Проверяется условие, затем если оно верно – возвращается значение1, если неверно – значение2 (тернарный оператор)
access = (age > 14) ? true : false; 
условие ? значение1 : значение2


27/10

логические операторы
|| (ИЛИ) - хотя бы один, 
&& (И) - оба
! (НЕ) - наоборот

28/10   + верстка header
итерация - повторение цикла
while(условие)
do{} while() - тело цикла
break - прервать цикл
continue - прекращает выполнение текущей итерации цикла
 
 
 31/10
 функции - function имяФункции(список_параметров){тело_функции}
 Для возврата значения используется директива return
 

1/11
Если функция объявлена в основном потоке кода, то это Function Declaration.
10/11
Если функция создана как часть выражения, то это Function Expression.

16/01
li - список
ul - маркированный список
aside - блок сбоку
form - форма (input - отправка на сервер)
method - сообщает серверу о методе запроса
placeholder - выводит текст внутри поля формы, который исчезает при получении фокуса.

